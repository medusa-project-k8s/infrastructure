apiVersion: v1
kind: ConfigMap
metadata:
  name: medusa-config
  namespace: medusa
data:
  POSTGRES_DB: "medusa-store"
  POSTGRES_USER: "postgres"
  NODE_ENV: "production"
  # Database connection will be set via environment variable using service name
  # DATABASE_URL will be constructed in the deployment
